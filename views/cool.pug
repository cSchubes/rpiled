extends ../jade-bootstrap/_bootstrap

append styles
    +bootswatch("darkly")
    
block body
    h1 header
    +a-btn-success("#") here is a button
    div
        input(data-wheelColorPicker="" data-wcp-layout="block" data-wcp-autoResize="true" type="text")
